#!/usr/bin/env bash
# bin/detect <build-dir>

build_dir=$1

if [ ! -f "$build_dir/static.json" ]; then
    echo "Could not find 'static.json'! So we do nothing with this build pack." >&2
    exit 0
fi

echo 'Static HTML'
exit 0
